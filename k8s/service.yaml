apiVersion: v1
kind: Service
metadata:
  name: retail-chatbot-svc
  labels:
    app: retail-chatbot
spec:
  type: ClusterIP
  selector:
    app: retail-chatbot
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
